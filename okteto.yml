build:
  eventhub-assistants:
    context: .


deploy:
  - helm upgrade --install contacts-service chart
    --set contacts.image=${OKTETO_BUILD_CONTACTS_SERVICE_IMAGE}

dev:
  eventhub-assistants:
    command: npm start
    sync:
      - .:/app